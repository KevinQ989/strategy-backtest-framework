[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "strategy_backtester"
version = "0.1.0"
description = "Strategy backtest framework for testing and evaluating trading strategies."
requires-python = ">=3.9"
dependencies = [
    "numpy",
    "matplotlib",
    "seaborn",
    "yfinance",
    "pandas",
    "pyyaml",
    "lxml"
]

[project.optional-dependencies]
dev = [
    "pytest",
    "pytest-cov",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["strategy_backtester*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.coverage.run]
omit = [
    "*/__init__.py",
    "strategy_backtester/main.py",
    "strategy_backtester/results/plots.py",
    "strategy_backtester/results/tearsheet.py",
]